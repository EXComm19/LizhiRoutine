{
  "name": "lizhi-routine-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that captures the current page as context for a Lizhi Routine todo.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@mozilla/readability": "^0.5.0",
    "@types/chrome": "^0.0.270",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
